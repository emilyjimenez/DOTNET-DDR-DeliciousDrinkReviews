@model IEnumerable<DDR.Models.Post>

@{

    ViewData["Title"] = "Home";
    Layout = "_Layout";

}


<h1>Blog Posts</h1>

@foreach (var post in Model)
{

    <ul>
    <li><h3>@Html.DisplayFor(modelpost => @post.Title) , @Html.ActionLink("Details", "Details", "Blog", new { id = @post.PostId})</h3></li>
    <p>[DELETE POST] [EDIT POST]</p>
    <p>[COMMENT]</p>
    </ul>
}

<p>@Html.ActionLink("Add Post", "Create", "Blog")</p>
<p>@Html.ActionLink("Go Back", "Index", "Home")</p>